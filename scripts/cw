#!/usr/bin/env bash

set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
CLI_BIN="${ROOT_DIR}/.venv/bin/cw"

if [ ! -x "${CLI_BIN}" ]; then
  echo "ContextWeave is not bootstrapped yet." >&2
  echo "Run: ./scripts/bootstrap" >&2
  exit 1
fi

exec "${CLI_BIN}" "$@"
